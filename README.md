# CPRG 216 – Assignment 3
## Car Inventory System

### Program Information
**Names:** Nathan Foran, Daniel Carpintero, Oluwalayomi Giwa  
**Date:** December 2025  
**Program:** Car Inventory System  

## Description
This program allows a car dealership to manage its inventory by adding, searching, editing, removing, printing, and saving car records.

Cars are stored in a list of `Car` objects and are saved to a local file (`data.txt`) so the inventory can be retrieved when the program is closed and reopened.

- **Inputs:** Car details entered by the user  
- **Processing:** Duplicate checks, file input/output, and menu-driven operations  
- **Outputs:** Confirmation messages and formatted car listings  

## Features
- Add a car to the inventory
- Search for a car by ID or name
- Edit car information
- Remove a car from the inventory
- Print the car list
- Save inventory data to a file
- Load saved data on program startup

## Files Included
- `Project.py` – Main Python program
- `data.txt` – Inventory data file (created when saving)
- `README.md` – Project documentation

## How to Run
1. Ensure Python is installed.
2. Place all project files in the same directory.
3. Open the project folder in a Python IDE or terminal.
4. Run the program using:

5. Follow the on-screen menu instructions.

## Requirements Met
- Uses classes and lists
- Correct data types (`int`, `float`, `string`)
- Menu-driven interface
- File saving and loading
- Matches the assignment scenario and test plan
